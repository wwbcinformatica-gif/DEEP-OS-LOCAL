# Historical and Completed Fixes
_Extracted from MEMORY.md — entries for completed work, known issues, and stable decisions unlikely to be revisited._

## Discovered

- **Task checklist system** (BROKEN — 2026-06-21): Two rendering paths: (1) Backend `task_checklist` events via `task_plan`/`task_progress` JSON — wired in `chat.py:750-817`, parsed from thinking content via regex. (2) Frontend `TaskChecklist.tsx` (180 lines) renders `[ ]`/`[~]`/`[x]`/`[!]` with line-through. `App.tsx:834` handles `task_checklist` events. **BUG**: Checkboxes stay at 0/4 even after tasks complete. `on_tool_start_fn`/`on_tool_end_fn` callbacks only fire when `model_sends_explicit_progress` is False — when model sends explicit `task_progress` JSON, auto-advance is disabled, but `task_progress` parsing only happens inside `thinking` events (chat.py:788). If content arrives as other event types, parsing never fires. Also: `on_tool_end_fn` doesn't yield a `task_checklist` event (only `on_tool_start_fn` does via `state_ref["yielded"]`). **Needs fix**: restructure so checklist events are yielded reliably after every tool lifecycle step.
- **Documentation consolidation**: All project .md docs consolidated into `manual/` folder. `manual/manual.md` = unified manual (14 sections). `manual/instrucoes.md` = user instructions (7 sections). Both have navigation links. Redundant root files to delete: `TESTE_TOOLCHAIN.md`, `TESTE_LOW.md`, `configuracao.md`, `manual/manual_usuario.md`. Standalone docs kept: `SQL_Complete_Guide.md`, `0-Projetos/WBC-Diag/README.md`, `project-loader/README.md`.
- **Admin.jsx critical issues (2026-06-22)**: Rules of Hooks violation fixed (hooks moved above conditional return). Duplicate UserFormModal fields fixed. alert() replaced with setMsg(). Bulk permission operations now have confirmation dialogs. Remaining: apiFetch unused, shared usuarios state, hardcoded niveis, empty catch blocks.
- **CSS specificity for module-level toggles**: `html.wbc-maiusculo-*` (specificity 0,1,2) overrides `.wbc-texto-maiusculo input` (specificity 0,1,1). Required because global toggle excludes inputs but per-module toggles must include them.
- **UserFormModal redundant field**: Both "Nome do Usuario" and "Login" were bound to `form.usuario` — only one field needed since the USUARIO table has a single name field.
- **Produtos.jsx missing focus**: Search input lacks `useRef` and auto-focus on mount. Known issue (2026-06-21), fix pending.
